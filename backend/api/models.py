# This file is kept for potential future use and to maintain backwards compatibility.

# Import models from other apps for convenience:
from chat.models import Conversation, Message
from flashcards.models import Flashcard
# ... import other models as needed

# Add any API-specific models below:
# (currently, there are no API-specific models)